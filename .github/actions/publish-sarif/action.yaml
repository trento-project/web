# SPDX-FileCopyrightText: SUSE LLC
# SPDX-License-Identifier: Apache-2.0

name: Publish SARIF
description: >-
  Upload a SARIF report as a workflow artifact and, when the token allows it,
  to GitHub code scanning.

inputs:
  name:
    description: Artifact name suffix. Must be unique within a workflow run.
    required: true
  path:
    description: Path to the SARIF file, relative to the workspace.
    required: true
  category:
    description: Code scanning category. Must be unique per tool.
    required: true
  optional:
    description: >-
      Whether a missing report is an expected outcome. Only true for scanners
      that write no file when they find nothing.
    required: false
    default: 'false'

runs:
  using: composite
  steps:
    # A missing report is only expected for scanners like Nuclei, which write
    # no file at all when there are no findings. For everything else, a
    # missing file means the scan died before writing anything, which must
    # not go unnoticed.
    - name: Check whether a report was produced
      id: check
      shell: bash
      env:
        SARIF_PATH: ${{ inputs.path }}
        SARIF_NAME: ${{ inputs.name }}
        OPTIONAL: ${{ inputs.optional }}
      run: |
        set -euo pipefail
        if [[ -f "$SARIF_PATH" ]]; then
          echo "exists=true" >> "$GITHUB_OUTPUT"
        elif [[ "$OPTIONAL" == "true" ]]; then
          echo "exists=false" >> "$GITHUB_OUTPUT"
          echo "::notice title=publish-sarif::No SARIF file at ${SARIF_PATH} for ${SARIF_NAME}; nothing to publish"
        else
          echo "::error title=publish-sarif::No SARIF file at ${SARIF_PATH} for ${SARIF_NAME}; the scan did not produce a report"
          exit 1
        fi

    - name: Upload SARIF artifact
      if: steps.check.outputs.exists == 'true'
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
      with:
        name: sarif-${{ inputs.name }}
        path: ${{ inputs.path }}
        retention-days: 30

    # Pull requests from forks receive a read-only GITHUB_TOKEN and cannot write
    # to code scanning. They fall through to artifact-only rather than failing.
    - name: Upload SARIF to code scanning
      if: >-
        steps.check.outputs.exists == 'true' &&
        (github.event_name != 'pull_request' ||
         github.event.pull_request.head.repo.full_name == github.repository)
      uses: github/codeql-action/upload-sarif@5595ccaf912efad79be6eef63a5619ff05969be3 # v4
      with:
        sarif_file: ${{ inputs.path }}
        category: ${{ inputs.category }}
